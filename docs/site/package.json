{
  "name": "free-rider-docs",
  "private": true,
  "scripts": {
    "dev": "vitepress",
    "prebuild": "node --test tests/*.test.mjs",
    "build": "vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "1.6.4"
  }
}
