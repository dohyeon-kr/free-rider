name: Publish documentation

on:
  push:
    branches: [main]
    paths:
      - "docs/site/**"
      - "src/**"
      - "build/icon-source/**"
      - ".github/workflows/docs.yml"
  pull_request:
    paths:
      - "docs/site/**"
      - "src/**"
      - "build/icon-source/**"
      - ".github/workflows/docs.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: github-pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install documentation dependencies
        run: npm install --prefix docs/site --no-package-lock

      - name: Test demo adapter and renderer build
        run: npm run --prefix docs/site test:demo

      - name: Build VitePress documentation
        run: npm run --prefix docs/site build

      - name: Install browser for demo verification
        working-directory: docs/site
        run: npx playwright install --with-deps chromium

      - name: Verify interactive demo in Chromium
        run: npm run --prefix docs/site smoke:demo

      - name: Upload demo screenshots and verification results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: renderer-demo-preview
          path: |
            docs/site/.vitepress/demo-artifacts
            docs/site/.vitepress/dist
          if-no-files-found: ignore
          retention-days: 7

      - name: Configure Pages
        if: github.event_name != 'pull_request'
        uses: actions/configure-pages@v5

      - name: Upload documentation site
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v4
        with:
          path: docs/site/.vitepress/dist

  deploy:
    if: github.event_name != 'pull_request'
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
